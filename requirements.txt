# core
streamlit==1.24.0

# computer vision - use headless opencv (compatible)
opencv-python-headless==4.8.1.78

# allow pip to pick a compatible numpy for the runtime (don't pin 1.26.0)
numpy>=1.25.0

# pillow must be < 10 to satisfy streamlit 1.24.0
Pillow>=9.5.0,<10

# other libs your project needs (examples)
pandas>=1.5
scikit-learn>=1.2
